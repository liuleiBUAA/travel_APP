# 工作日志 2026-04-03

## 主要工作

### 1. 修复城市→国家匹配的误判问题

**问题：**
- 子串匹配导致误判：`"尼斯" in "威尼斯"` → 错误匹配意大利
- 输入"尼斯"+"里昂"返回 `['意大利', '法国']`（应该只返回法国）

**解决方案：**
- 在 `recommend_smart.py` 添加 `load_city_mapping()` 函数（复用 travel_guide 现有逻辑）
- 在 `route_service.py` 加载 city_mapping
- 修改 `_extract_countries_from_cities()` 匹配逻辑：
  1. 先用 mapping 映射（"尼斯" → "蔚蓝海岸（尼斯/摩纳哥/戛纳）"）
  2. 精确匹配 destinations
  3. fallback 到子串匹配

**测试结果：**
- ✅ "尼斯"+"里昂" → `['法国']`（修复成功）
- ✅ "威尼斯"+"罗马" → `['意大利']`
- ✅ "巴黎"+"尼斯"+"威尼斯" → `['意大利', '法国']`

### 2. 集成路线自动验证功能

**目标：**
路线生成后自动验证质量，不需要手动运行 `verify_routes.py`

**实现：**

1. **创建独立验证模块**
   - 新建 `travel_guide/src/core/route_validator.py`
   - 从 `verify_routes.py` 提取核心验证逻辑
   - 可被 `route_planner.py` 直接调用

2. **集成到路线生成器**
   - `route_planner.py`：
     - 导入 `validate_route`
     - 在 `plan()` 方法保存文件后自动调用验证
     - 返回值新增 `validation` 字段（向后兼容）
     - 输出验证结果到 markdown

3. **API 层传递验证结果**
   - `route_service.py`：
     - destination 模式：从 `plan_result` 提取 `validation`
     - recommend 模式：从 `route_text` 提取 `validation`
     - 返回给前端完整验证结果

**验证内容：**

*格式检查（4项）：*
- 交通时间显示错误（-h/Noneh）
- Day 编号连续性
- 重复地名检查
- 占位符残留

*质量检查（3项）：*
- 走回头路检测
- 起终点是否为 hub 城市
- 停留天数是否合理

**测试结果：**
```bash
# destination 模式
路线: 阿姆斯特丹 → 巴黎 → 布鲁塞尔
状态: ok
✅ 无警告

# recommend 模式
路线: 特内里费岛 → 马略卡岛 → 普罗旺斯
状态: warning
警告数: 3（起终点非hub城市等）
```

### 3. 修复 hub_cities.json 路径问题

**问题：**
`route_validator.py` 中 hub 文件路径错误：
```python
# 错误：data/{region}/config/hub_cities.json
# 实际：data/{region}/hub_cities.json
```

**修复：**
更正路径为 `data/{region}/hub_cities.json`

## 文件变更

### 新增文件
- `travel_guide/src/core/route_validator.py` - 独立验证模块

### 修改文件
1. `travel_guide/src/core/recommend_smart.py`
   - 新增 `load_city_mapping()` 函数

2. `travel_guide/src/core/route_planner.py`
   - 导入 `validate_route`
   - plan() 方法集成自动验证
   - 返回值新增 `validation` 字段（向后兼容）

3. `backend/services/route_service.py`
   - 导入 `load_city_mapping`
   - `__init__` 加载 city_mapping
   - 修改 `_extract_countries_from_cities()` 匹配逻辑
   - destination/recommend 模式都传递 validation 结果

## 兼容性说明

✅ **所有修改向后兼容**

- `route_planner.py` 只新增返回字段，不影响现有调用
- 原有代码可以忽略 `validation` 字段继续使用
- 路线生成逻辑未改变

## 下一步

- [ ] 前端/小程序展示验证结果（optional）
- [ ] 根据验证结果自动调整路线（optional）

## 测试命令

```bash
# 测试 destination 模式
curl -X POST http://localhost:8000/api/routes/generate \
  -H "Content-Type: application/json" \
  -d '{"mode":"destination","cities":["巴黎","阿姆斯特丹"]}'

# 测试 recommend 模式
curl -X POST http://localhost:8000/api/routes/generate \
  -H "Content-Type: application/json" \
  -d '{"mode":"recommend","region":"Europe","travel_month":6,"duration_days":10}'

# 测试搜索（验证 countries 字段）
curl "http://localhost:8000/api/companions/search?keyword=法国"
```
