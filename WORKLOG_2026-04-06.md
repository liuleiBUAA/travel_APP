# 工作日志 2026-04-06

## 主要工作

### 1. 完整参数对齐：travel_guide → route_service → API → 前端 → 小程序

**背景：** 发现各层参数未完全透传，导致用户设置的参数在部分模式下被忽略。

**修复内容：**

| 问题 | 修复文件 | 内容 |
|------|---------|------|
| destination 模式未传 region | `route_service.py` / `main.py` / `frontend` / `miniprogram` | generate_from_cities 新增 region 参数，四层全部透传 |
| recommend 模式未传 force 三参数 | `route_service.py` / `main.py` / `frontend` / `miniprogram` | recommend_route 新增 force_gateway_departure / force_order / same_day_max_hours，四层全部透传 |

---

### 2. 修复到达日不走 hub 城市的 bug

**问题：** `force_gateway_departure` 原来只管离开日，到达日没有 hub 兜底逻辑（五渔村/西西里等可以直接飞入）。

**修复：** `route_planner.py` 的 `_add_arrival_departure()` 新增与离开日对称的逻辑：
- 到达日优先选 MAJOR_HUBS 中的城市
- 无 hub 时按坐标距离找最近 hub
- 城市无坐标时用依赖城市（帕尔马代替马略卡岛）的坐标

---

### 3. Harness 传感器扩展

**修复：** lessons-learned.md 解析 bug（`split(":**")` → `split("**: ")`）

**新增 `validate_route_json()` 业务验证：**
- 必要字段检查：cities / total_days / city_count / itinerary / route_type
- itinerary 条目结构：day / activity / transport / stay
- hub 城市约束：generated/recommended 路线的到达日和离开日必须经过 MAJOR_HUBS
- simple 降级检测：自动记录 violation 并触发经验提示

**接入位置：** `main.py` 的 `generate_route` 端点在返回前自动调用，形成闭环传感器。

---

## 当前对齐状态

### 已对齐（5 个核心参数）

| 参数 | config.json | route_service | main.py | frontend | 小程序 |
|------|------------|---------------|---------|----------|--------|
| same_day_max_hours | ✅ | ✅ | ✅ | ✅ | ✅ |
| force_gateway_departure | ✅ | ✅ | ✅ | ✅ | ✅ |
| force_order | ✅ | ✅ | ✅ | ✅ | ✅ |
| region | ✅ | ✅ | ✅ | ✅ | ✅ |
| destinations | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 明天待处理：3 个残留缺口

### 缺口 1：transport_preference 全层缺失
- **config.json 有：** `"transport_preference": "auto"`（值：auto / train / flight）
- **四层全无：** route_service / main.py API / frontend / 小程序 都没有这个参数
- **影响：** 用户无法指定偏好交通方式（自动/优先火车/优先飞机）
- **需要改：** generate_from_cities + recommend_route 加参数 → API Request 加字段 → 前端加下拉 → 小程序加控件

### 缺口 2：options_display_mode 全层缺失
- **config.json 有：** `"options_display_mode": "compact"`（值：compact / detailed）
- **四层全无：** 同上
- **影响：** 行程显示详细程度不可控
- **需要改：** 同 transport_preference，四层同步补齐

### 缺口 3：start_node / end_node 前端无入口
- **API 有：** `RouteGenerateRequest` 已有 `start_node` / `end_node` 字段，destination 模式也透传给 generate_from_cities
- **前端/小程序无：** 用户无法指定起止城市
- **影响：** destination 模式下路线起止城市由算法自动决定，用户无法控制
- **需要改：** frontend 和小程序各加一个可选输入框

---

## 文件修改清单

| 文件 | 修改内容 |
|------|---------|
| `travel_guide/src/core/route_planner.py` | 到达日 hub 兜底逻辑（两轮修复） |
| `backend/services/route_service.py` | generate_from_cities 加 region；recommend_route 加 force 三参数 |
| `backend/main.py` | 两个模式透传新参数；接入 harness.validate_route_json |
| `backend/harness/__init__.py` | 修复 lessons 解析 bug；扩展 validate_route_json |
| `frontend/index.html` | destination 加 region；recommend 加 force 三参数 |
| `miniprogram/pages/guide/guide.js` | destination 加 region；recommend 加 force 三参数 |
| `backend/.harness/lessons-learned.md` | 新增 6 条经验（含今天的 3 个缺口） |
